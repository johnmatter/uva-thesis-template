\section{Event Selection}
Several cuts were placed to select true $ep$ coincidences in the
analysis of ${}^{12}C(e,e'p)$ and $H(e,e'p)$ data.
They are listed in Tables~\ref{tab:lh2_cuts} and \ref{tab:c12_cuts}.
Most of the cuts are identical for the two targets--the only difference is the
limits of cuts placed on $E_{miss}$ and $p_{miss}$.
The cuts on delta in both spectrometers ensure that the events used are in a
central region of momentum acceptance with acceptable optics reconstruction.
Cuts on normalized calorimeter energy and Cherenkov photoelectrons select
electrons in the HMS and remove negatively charged pions.
Similarly, the noble gas Cherenkov in the SHMS acts as a pion-veto to limit
SHMS events to protons.
The cuts on $E_{miss}$ and $p_{miss}$ were selected
for consistency with prior measurements and
to keep the center of mass energy below the threshold for pion creation.

\begin{table}[h]
    \centering
    \caption{List of cuts used to select clean $H(e,e'p)$ events.}
    \label{tab:lh2_cuts}
    \resizebox{1.0\textwidth}{!}{
        \begin{tabular}[t]{ l  l }
\specialrule{.1em}{.05em}{.05em}
            Variables              &  Cut \\
\specialrule{.1em}{.05em}{.05em}
            HMS Calorimeter Energy &  0.8 < H.cal.etottracknorm \&\& H.cal.etottracknorm < 1.15 \\ \hline
            HMS Cherenkov NPE      &  H.cer.npeSum>0                                            \\ \hline
            SHMS NG Cherenkov Veto &  P.ngcer.npeSum<0.1                                        \\ \hline
            $\delta_{HMS}$         &  -8.0  < H.gtr.dp \&\& H.gtr.dp <  8.0                     \\ \hline
            $\delta_{SHMS}$        &  -10.0 < P.gtr.dp \&\& P.gtr.dp < 15.0                     \\ \hline
            $\beta_{HMS}$          &  0.6 < H.gtr.beta \&\& H.gtr.beta < 1.4                    \\ \hline
            $\beta_{SHMS}$         &  0.8 < P.gtr.beta \&\& P.gtr.beta < 1.4                    \\ \hline
            $E_{miss}$             &  P.kin.secondary.emiss<0.1                                 \\ \hline
            $p_{miss}$             &  abs(P.kin.secondary.pmiss)<0.1                            \\ \hline
            $p_{miss}$             &  -0.1 < P.kin.secondary.pmiss \&\& P.kin.secondary.pmiss < 0.1 \\ \hline
            Good hodoscope times   &  H.hod.goodstarttime==1 \&\& P.hod.goodstarttime==1       \\
\specialrule{.1em}{.05em}{.05em}
        \end{tabular}
    }
\end{table}

\begin{table}[h]
    \centering
    \caption{List of cuts used to select clean ${}^{12}C(e,e'p)$ events.}
    \label{tab:c12_cuts}
    \resizebox{1.0\textwidth}{!}{
        \begin{tabular}[t]{ l  l }
           \specialrule{.1em}{.05em}{.05em}
            Variables              &  Cut \\
            \specialrule{.1em}{.05em}{.05em}
            HMS Calorimeter Energy &  0.8 < H.cal.etottracknorm \&\& H.cal.etottracknorm < 1.15 \\ \hline
            HMS Cherenkov NPE      &  H.cer.npeSum>0                                            \\ \hline
            SHMS NG Cherenkov Veto &  P.ngcer.npeSum<0.1                                        \\ \hline
            $\delta_{HMS}$         &  -8.0  < H.gtr.dp \&\& H.gtr.dp <  8.0                     \\ \hline
            $\delta_{SHMS}$        &  -10.0 < P.gtr.dp \&\& P.gtr.dp < 15.0                     \\ \hline
            $\beta_{HMS}$          &  0.6 < H.gtr.beta \&\& H.gtr.beta < 1.4                    \\ \hline
            $\beta_{SHMS}$         &  0.8 < P.gtr.beta \&\& P.gtr.beta < 1.4                    \\ \hline
            $E_{miss}$             &  P.kin.secondary.emiss<0.08                                \\ \hline
            $p_{miss}$             &  -0.3 < P.kin.secondary.pmiss \&\& P.kin.secondary.pmiss < 0.3 \\ \hline
            Good hodoscope times   &  H.hod.goodstarttime==1 \&\& P.hod.goodstarttime==1        \\
            \specialrule{.1em}{.05em}{.05em}
        \end{tabular}
    }
\end{table}
